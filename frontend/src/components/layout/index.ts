// Layout Components
export { default as Header } from './Header';
export { default as Sidebar } from './Sidebar';

// Re-export types if needed
export type { HeaderProps, SidebarProps } from './types';
